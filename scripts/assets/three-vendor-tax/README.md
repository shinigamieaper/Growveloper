# Three-Vendor Tax — Image Assets

Drop these three files here before running `node scripts/seed-three-vendor-tax.mjs`:

| Filename | Description |
|---|---|
| `hero.png` | The big "$190,000/yr" essay opener (hero image) |
| `cost-bar-chart.png` | The horizontal bar chart "Where the $190K goes" (inline body image) |
| `four-costs-grid.png` | The 2x2 grid showing Rebuild / Gap / Standstill / Translation (inline body image) |

PNG or JPG both accepted. Update the `IMAGE_FILES` map in the seed script if you change names.
